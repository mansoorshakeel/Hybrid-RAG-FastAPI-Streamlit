<<<<<<< HEAD
# RAG Backend (FastAPI + LangChain + Pinecone)

## 1) Setup (local)

```bash
python -m venv .venv
# Windows (PowerShell)
.venv\Scripts\Activate.ps1
pip install -r requirements.txt
||||||| parent of a32b280 (Update hybrid branch files (backend docs, tests, ignore rules))
=======
# RAG Backend (FastAPI + LangChain + Pinecone)
This is the Streamlit UI for the RAG chatbot. It calls a FastAPI backend over HTTP.
## 1 Setup (local)

```bash
python -m venv .venv
# Windows (PowerShell)
.venv\Scripts\Activate.ps1
pip install -r requirements.txt
>>>>>>> a32b280 (Update hybrid branch files (backend docs, tests, ignore rules))
